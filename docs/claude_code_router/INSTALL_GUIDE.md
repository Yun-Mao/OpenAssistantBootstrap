# Claude Code Router 详细安装指南

## 目录

- [项目概述](#项目概述)
- [系统要求](#系统要求)
- [准备工作](#准备工作)
- [安装步骤](#安装步骤)
- [配置环境](#配置环境)
- [验证安装](#验证安装)
- [常见问题](#常见问题)
- [卸载说明](#卸载说明)

## 项目概述

**Claude Code Router** 是一个社区开源项目（由 musistudio 维护），用于将 Claude Code 请求智能路由到不同的 AI 模型提供商。它是一个 **Node.js 应用程序**，提供强大的模型路由、请求转换和多提供商管理能力。

**官方项目**: [musistudio/claude-code-router](https://github.com/musistudio/claude-code-router)  
**GitHub Stars**: 28.4k+  
**许可证**: MIT

### 核心特性

✅ **多提供商支持** - OpenRouter、DeepSeek、Ollama、Gemini、Volcengine、SiliconFlow 等  
✅ **灵活路由** - 基于场景路由（默认、后台任务、思考、长上下文、网络搜索等）  
✅ **请求转换** - 自定义请求/响应转换器，确保不同提供商的兼容性  
✅ **动态模型切换** - 使用 `/model` 命令快速切换  
✅ **CLI 和 UI 管理** - 通过命令行或网页界面管理配置  
✅ **活跃社区** - 定期更新，广泛的社区支持

## 系统要求

### 硬件要求

- **CPU**: x86_64 或 ARM64 (aarch64) 架构
- **内存**: 至少 512MB RAM
- **磁盘空间**: 至少 200MB 自由空间

### 软件要求

- **OS**: CentOS 7, RHEL, Ubuntu, Debian 等 Linux 发行版
- **Node.js**: 16.0.0+ （推荐 18 LTS 或更高）
- **npm**: 8.0.0+ 或 pnpm 8.0.0+
- **权限**: 普通用户权限即可（无需 root）

### 前置条件

**必需**:
- ✅ Claude Code（官方工具）需先安装
  ```bash
  npm install -g @anthropic-ai/claude-code
  ```

### 网络

- 安装时需网络连接（npm 包下载）
- 或提前下载 npm 包到本地离线安装

## 准备工作

### 1. 确保 Node.js 已安装

```bash
# 检查 Node.js 版本
node --version    # 应输出 v16.0.0+
npm --version     # 应输出 8.0.0+
```

如未安装，参考 [Node.js 安装指南](../nodejs/INSTALL_GUIDE.md)。

### 2. 确保 Claude Code 已安装

```bash
# 检查 Claude Code
claude --version  # 应输出版本号
```

如未安装：
```bash
npm install -g @anthropic-ai/claude-code
```

### 3. 获取 Claude Code Router 源

**在线安装**（推荐）:
```bash
# 直接从 npm 官方源安装
npm install -g @musistudio/claude-code-router
```

**离线安装** - 三个选项：

#### 方案 A: npm tarball 离线包
在有网络的机器上下载 npm 包：
```bash
# 下载到 packages 目录
npm pack @musistudio/claude-code-router --pack-destination ./packages/
# 输出: packages/musistudio-claude-code-router-1.0.40.tgz
```

#### 方案 B: 本项目的 fetch 脚本
```bash
# 编写一个 fetch 脚本（类似 fetch_claude_code.sh）
# 来下载最新的 npm 包
```

#### 方案 C: 手动克隆源代码
```bash
# 在有网络的机器上
git clone https://github.com/musistudio/claude-code-router.git
cd claude-code-router
npm install
npm run build
# 将项目目录复制到离线机器
```

### 4. 准备安装环境

```bash
# 1. 进入项目目录
cd OpenAssistantBootstrap

# 2. 赋予安装脚本执行权限
chmod +x scripts/install_claude_code_router.sh

# 3. （可选）将离线包放入 packages 目录
# 用于离线安装或加速安装
cp /path/to/musistudio-claude-code-router-*.tgz packages/

# 4. 验证环境
node --version && npm --version && claude --version
```

## 安装步骤

### 方法一：直接 npm 安装（推荐）

最简单快速的方式：

```bash
# 全局安装
npm install -g @musistudio/claude-code-router

# 验证安装
ccr --version
```

### 方法二：交互式安装脚本

使用本项目提供的交互式脚本：

```bash
# 运行安装脚本
./scripts/install_claude_code_router.sh
```

**交互式流程说明**:

#### 主菜单
```
╔═════════════════════════════════════╗
║  Claude Code Router 管理工具      ║
╚═════════════════════════════════════╝

选择操作模式:

  1. 安装 Claude Code Router
  2. 卸载 Claude Code Router
  3. 更新配置
  0. 退出
```

选择 **1**（安装）开始。

#### 步骤说明

**步骤 1: 检测现有安装**
```
┌─────────────────────────────────────┐
│  步骤 1: 检测现有安装              │
└─────────────────────────────────────┘

[INFO] 未检测到现有 Claude Code Router 安装

  是否继续安装新版本? (y/N):
```

输入 `y` 并回车继续。

**步骤 2: 选择安装路径**
```
┌─────────────────────────────────────┐
│  步骤 2: 选择安装路径              │
└─────────────────────────────────────┘

  默认路径: /home/user/claude-code-router
  输入安装路径（直接回车使用默认）:
```

- 直接回车使用默认路径 `$HOME/claude-code-router`
- 或输入自定义路径，如 `/data/software/ccr`

**步骤 3: 选择压缩包**
```
┌─────────────────────────────────────┐
│  步骤 3: 选择压缩包                │
└─────────────────────────────────────┘

  找到默认压缩包:
    claude-code-router-1.0.0-x86_64-linux.tar.gz
  使用此文件? (Y/n):
```

- 直接回车或输入 `Y` 使用找到的文件
- 输入 `n` 手动指定其他路径

**步骤 4: 确认安装信息**
```
┌─────────────────────────────────────┐
│  步骤 4: 确认安装信息              │
└─────────────────────────────────────┘

  源包: packages/claude-code-router-1.0.0-x86_64-linux.tar.gz
  目标路径: /home/user/claude-code-router

  确认开始安装? (y/N):
```

输入 `y` 确认开始安装。

**步骤 5-9: 自动执行**

脚本将自动：
- 检查目标路径（存在时确认覆盖）
- 创建安装目录
- 解压压缩包
- 复制文件
- 设置文件权限
- 验证关键文件

预计耗时 30 秒 ~ 2 分钟（取决于网络和磁盘速度）。

**步骤 10: 安装完成**
```
┌─────────────────────────────────────┐
│  步骤 10: 安装完成                 │
└─────────────────────────────────────┘

[SUCCESS] 安装路径: /home/user/claude-code-router
[SUCCESS] Claude Code Router 版本: 1.0.0

┌─────────────────────────────────────┐
│  后续配置步骤                      │
└─────────────────────────────────────┘

  是否自动配置环境变量到 ~/.bashrc? (y/N):
```

输入 `y` 自动配置环境变量（推荐），这样之后可以直接在任何目录使用 `claude-code-router` 命令。

### 方法二：查看帮助

```bash
# 显示使用说明
./scripts/install_claude_code_router.sh --help
```

## 配置环境

### 自动配置（推荐）

如果在安装时选择了自动配置，脚本已为您处理：

```bash
# 应用配置
source ~/.bashrc

# 验证 PATH
echo $PATH
```

### 手动配置

如果需要手动配置环境变量：

```bash
# 编辑 .bashrc
nano ~/.bashrc

# 在文件最后添加（替换 /path/to/claude-code-router）：
export PATH="/path/to/claude-code-router/bin:$PATH"

# 保存并退出（Ctrl+O, Enter, Ctrl+X）

# 应用配置
source ~/.bashrc
```

### 验证配置

```bash
# 检查环境变量是否正确设置
echo $PATH | grep claude-code-router

# 检查命令是否可用
which claude-code-router

# 查看版本
claude-code-router --version
```

## 验证安装

### 1. 检查安装目录

```bash
# 列出安装目录内容
ls -la ~/claude-code-router/

# 输出应该包含：
# drwxr-xr-x  bin/
# drwxr-xr-x  lib/
# -rw-r--r--  README.md
# -rw-r--r--  LICENSE
```

### 2. 验证可执行文件

```bash
# 检查主执行文件
file ~/claude-code-router/bin/claude-code-router

# 输出示例：
# .../claude-code-router: ELF 64-bit LSB executable, x86-64, ...

# 确认可执行
~/claude-code-router/bin/claude-code-router --version
```

### 3. 完整功能测试

```bash
# 显示帮助信息
claude-code-router --help

# 列出可用命令
claude-code-router -h

# 查看版本信息
claude-code-router --version

# 查看配置
claude-code-router config show  # 如果支持
```

## 常见问题

### Q1: 找不到压缩包怎么办？

**错误信息**:
```
[ERROR] 必须指定压缩包路径
```

**解决方案**:
1. 检查 packages 目录是否存在压缩包
   ```bash
   ls -lh packages/
   ```

2. 确保文件名匹配预期格式：
   ```bash
   # 应该类似于：
   claude-code-router-1.0.0-x86_64-linux.tar.gz
   ```

3. 手动指定路径：
   ```bash
   # 在提示时输入完整路径
   /home/user/Downloads/claude-code-router-1.0.0-x86_64-linux.tar.gz
   ```

### Q2: 权限错误

**错误信息**:
```
Permission denied
```

**解决方案**:
```bash
# 确保脚本有执行权限
chmod +x scripts/install_claude_code_router.sh

# 确保 packages 目录可读
chmod +r packages/claude-code-router-*.tar.gz

# 如果安装路径权限有问题，选择用户有权限的目录
# 例如：$HOME/claude-code-router
```

### Q3: 关键文件缺失

**错误信息**:
```
[ERROR] 错误: 未找到 claude-code-router 可执行文件
```

**解决方案**:
1. 检查压缩包完整性：
   ```bash
   # 查看压缩包内容
   tar -tzf packages/claude-code-router-*.tar.gz | head -20
   ```

2. 确认包结构正确：
   ```bash
   # 应该包含 bin/claude-code-router
   tar -tzf packages/claude-code-router-*.tar.gz | grep "bin/claude-code-router"
   ```

3. 重新下载官方包

### Q4: 安装后无法使用

**错误信息**:
```
claude-code-router: command not found
```

**解决方案**:
```bash
# 应用环境变量配置
source ~/.bashrc

# 验证环境变量
echo $PATH

# 直接使用完整路径测试
~/claude-code-router/bin/claude-code-router --version

# 如果可用，说明是环境变量问题，重新配置
echo 'export PATH="$HOME/claude-code-router/bin:$PATH"' >> ~/.bashrc
source ~/.bashrc
```

### Q5: 安装记录丢失

**情况**: 安装后删除了 `~/.claude_code_router_install_record`

**解决方案**:
```bash
# 重新创建记录
echo "/home/user/claude-code-router" > ~/.claude_code_router_install_record

# 或重新运行安装脚本（选择卸载后再安装）
```

## 卸载说明

### 使用脚本卸载（推荐）

```bash
# 运行卸载模式
./scripts/install_claude_code_router.sh

# 在主菜单选择 2（卸载）
# 脚本将：
# - 删除安装目录
# - 删除环境变量配置
# - 删除安装记录
```

### 手动卸载

```bash
# 1. 删除安装目录
rm -rf ~/claude-code-router

# 2. 从 .bashrc 中删除环境变量配置
nano ~/.bashrc
# 删除包含 claude-code-router 的行

# 3. 保存并应用
source ~/.bashrc

# 4. 删除安装记录
rm -f ~/.claude_code_router_install_record
```

### 验证卸载

```bash
# 检查命令是否仍可用（应该不可用）
claude-code-router --version

# 检查目录是否存在（应该不存在）
ls -la ~/claude-code-router

# 检查环境变量中是否仍有引用
echo $PATH | grep claude-code-router
```

## 故障排除

### 收集诊断信息

如果遇到问题，收集以下信息有助于快速定位：

```bash
# 系统信息
uname -a
lsb_release -a

# Bash 版本
bash --version

# 压缩包信息
ls -lh packages/claude-code-router-*.tar.gz

# 日志文件
tail -50 /tmp/claude_code_router_install_*.log

# 安装路径状态
ls -la ~/claude-code-router/bin/ 2>/dev/null || echo "目录不存在"

# 文件类型检测
file ~/claude-code-router/bin/claude-code-router 2>/dev/null
```

### 查看完整日志

```bash
# 最新安装日志
cat /tmp/claude_code_router_install_*.log | tail -100

# 查看所有日志
ls -lt /tmp/claude_code_router_install_*.log
```

## 进阶配置

### 多版本管理

```bash
# 安装不同版本到不同目录
./scripts/install_claude_code_router.sh
# 选择路径：/home/user/claude-code-router-1.0
# 重复流程，选择路径：/home/user/claude-code-router-1.1

# 切换版本
export PATH="/home/user/claude-code-router-1.1/bin:$PATH"
```

### 系统全局安装（使用 sudo）

如果有 sudo 权限，可以安装到系统目录：

```bash
# 注意：需要 sudo 权限
sudo mkdir -p /opt/claude-code-router
sudo chmod 755 /opt/claude-code-router

# 修改脚本中的 INSTALL_PATH 或在提示时输入
./scripts/install_claude_code_router.sh
# 在步骤 2 中输入：/opt/claude-code-router
```

## 获取帮助

- 🔍 查看脚本帮助: `./scripts/install_claude_code_router.sh --help`
- 📋 查看交互式指南: 参考 [INTERACTIVE_INSTALL.md](INTERACTIVE_INSTALL.md)
- ⚡ 快速参考: 参考 [QUICK_REFERENCE.md](QUICK_REFERENCE.md)
- 📚 官方文档: [Claude Code Router 官方仓库](https://github.com/anthropic/claude-code-router)

---

**最后更新**: 2026年2月  
**作者**: OpenAssistantBootstrap 维护团队  
**许可证**: MIT
